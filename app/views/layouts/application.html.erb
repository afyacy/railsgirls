<!DOCTYPE html>
<html>
  <head>
    <title>Railsgirls</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/css/bootstrap.min.css">
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
      <a class="navbar-brand" href="/">The Idea App</a>
      <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarTogglerDemo02" aria-controls="navbarTogglerDemo02" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>

      <div class="collapse navbar-collapse" id="navbarTogglerDemo02">
        <ul class="navbar-nav mr-auto mt-2 mt-lg-0">
          <li class="nav-item active">
            <a class="nav-link" href="/ideas">Ideas <span class="sr-only">(current)</span></a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="/pages/info">Info</a>
          </li>
        </ul>
        <div class="form-inline my-2 my-lg-0">
          <% if user_signed_in? %>
          <span class="text-white mr-2">Logged in as:  <em><%= current_user.email %></em></span>
          <span><%= link_to 'Edit profile', edit_user_registration_path, :class => 'navbar-link text-white bg-primary p-2 rounded mr-4' %></span>
          <%= link_to "Logout", destroy_user_session_path, method: :delete, :class => 'navbar-link text-danger rounded mr-4'  %>
          <%= image_tag current_user.gravatar_url, :class => "gravatar" %>
          <% else %>
            <%= link_to "Sign up", new_user_registration_path, :class => 'navbar-link text-white bg-primary p-2 rounded mr-4'  %> |
            <%= link_to "Login", new_user_session_path, :class => 'navbar-link text-white bg-success p-2 rounded mr-4'  %>
          <% end %>
        </div>
      </div>
    </nav>

    <div class="container">
    <% if notice %>
      <p class="alert alert-success"><%= notice %></p>
    <% end %>
    <% if alert %>
      <p class="alert alert-danger"><%= alert %></p>
    <% end %>
      <%= yield %>
    </div>
    <footer>
      <div class="container">
        Rails Girls <%= Time.now.year %>
      </div>
    </footer>
    <script src="https://code.jquery.com/jquery-3.3.1.slim.min.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/popper.js/1.14.3/umd/popper.min.js"></script>
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.1.3/js/bootstrap.min.js"></script>
  </body>
</html>
